# Track versioned MSIX binaries with Git LFS to keep git history lean.
# Run `git lfs install` once before your first push if LFS isn't set up yet.
gh-pages/versions/*.msix filter=lfs diff=lfs merge=lfs -text
