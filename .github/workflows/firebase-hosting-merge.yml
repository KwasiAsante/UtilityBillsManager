# This file was auto-generated by the Firebase CLI and updated to use Flutter.
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Create local_secrets.json placeholder
        run: |
          mkdir -p assets/config
          echo '{}' > assets/config/local_secrets.json

      - name: Build Flutter web
        run: flutter build web --release

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_UTILITYBILLS_C942A }}
          channelId: live
          projectId: utilitybills-c942a
